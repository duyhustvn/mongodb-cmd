mongodb:
  host: 172.17.0.1:27017
  username: admin
  password: changeme

# Database lưu snapshot index stats — dùng lại cụm mongodb bên trên
storage:
  database: mongo_monitoring

# Cấu hình background collector
snapshot:
  interval_minutes: 60  # chụp snapshot mỗi 60 phút

server:
  port: 8082